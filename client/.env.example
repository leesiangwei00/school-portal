# Leave unset for local dev — Vite proxies /api to http://localhost:4000 (see vite.config.ts).
# In production (Vercel), set this to your deployed Render API URL, e.g.:
# VITE_API_BASE_URL=https://school-portal-api.onrender.com/api
VITE_API_BASE_URL=
